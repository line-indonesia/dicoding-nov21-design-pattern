enableFeaturePreview("VERSION_CATALOGS")

rootProject.name = "medan-perang-tidak-diketahui"

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            alias("junit").to("org.junit.jupiter:junit-jupiter-api:5.8.1")
        }
    }
}